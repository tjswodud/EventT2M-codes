# @package _global_

defaults:
  - _self_
  - data: hml3d_event_final # choose datamodule with `test_dataloader()` for evaluation
  - model: event_final
  - logger: null
  - trainer: gpu
  - paths: default
  - extras: default
  - hydra: default

task_name: "test_speed"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: "checkpoints/hml3d.ckpt"

# seed for random number generators in pytorch, numpy and python.random
seed: null

data_dir: "./dataset/HumanML3D"


repeats: 20

device: "0"

random_selected_file: "./dataset/random_selected_data.npy"

long_motion_idx: M005531

test_mode: random_motion

tgt_length: "none"

